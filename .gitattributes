*.zip filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text

# 讓 Git 自動統一換行符號 (保護 Mac/Linux 不被 Windows 格式干擾)
* text=auto

# 強制 .sh 腳本一定要用 LF (避免在 Mac/Linux 執行失敗)
*.sh text eol=lf